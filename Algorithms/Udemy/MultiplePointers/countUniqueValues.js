// Implement a function called countUniqueValues which accepts
// a sorted arrayBuffer, and counts the unique values in the array.
// There can be negative numbers in the array, but it will always be
// sorted.

function countUniqueValues(arr){

}

countUniqueValues([-4,-2,-1,0,1,2,5,6])